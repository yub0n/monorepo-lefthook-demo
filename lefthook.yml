pre-commit:
  skip:
    - merge
    - rebase
  commands:
    check:
      glob: "*.{js,ts,jsx,tsx,json,jsonc}"
      run: node scripts/dispatch-by-package.js check:staged {staged_files}
      stage_fixed: true
